{
  "name": "alexandria",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build-cli": "yarn workspace @alexandria/cli build",
    "apply-infra": "yarn workspace @alexandria/infrastructure deploy",
    "build-backend": "yarn workspace @alexandria/functions build",
    "deploy-backend": "yarn build-backend && yarn workspace @alexandria/functions push-images-processing && yarn apply-infra",
    "build-frontend": "yarn workspace @alexandria/web-client-v2 build",
    "deploy-frontend": "yarn workspace @alexandria/web-client-v2 deploy && yarn workspace @alexandria/infrastructure invalidate-frontend",
    "frontend-dev": "yarn workspace @alexandria/web-client-v2 dev"
  },
  "workspaces": [
    "packages/functions",
    "packages/web-client",
    "packages/web-client-v2",
    "packages/infrastructure",
    "packages/cli"
  ],
  "devDependencies": {}
}
